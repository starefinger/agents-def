---
description: 在 Cursor 工作区维护 ~/.config/opencode 全局配置仓库（Morning Star harness）时的边界与变更流程
alwaysApply: true
---

# Morning Star Harness 配置仓库维护（OpenCode / Cursor）

当工作区为 `~/.config/opencode/`（全局 OpenCode 配置仓库，Morning Star / 启明星 harness）时适用本规则。本规则**不**替代其他仓库中的项目级 `AGENTS.md`。

## 维护话术与清单的唯一入口

下列内容**仅在本文件**展开：**本配置仓库如何维护**、**更新规则**、**设计审查清单**、**避免与执行向正文双轨**。  
**不**在根目录 `AGENTS.md`、`skills/mstar-*/`、`agents/*.md` 中重复上述维护话术或指向本文件的说明。

**例外（仍保留在执行向文档中）**：与运行时行为直接相关的边界陈述，例如「全局配置落盘仅由用户执行」「未经同意不得改 `opencode.json` / 凭据」等。

**人类读者 / 非 Cursor 宿主**：用编辑器或 Read 打开本路径即可：`.cursor/rules/opencode-config-repo-maintenance.mdc`。

### 仓库内文件分工（索引）

| 路径 | 用途 |
|------|------|
| `AGENTS.md` | **Morning Star harness** 全局入口（执行向优先级、不变量、skill 索引、护栏）；OpenCode 每会话加载见 [Global Rules](https://opencode.ai/docs/rules/#global)；Cursor 等见 `.cursor/skills/mstar-host/SKILL.md` |
| `skills/mstar-harness-core/` | 核心不变量：状态机、Spec-Driven 双阶段门禁、分支 / worktree、QC-QA 检出对齐、调度防串扰、升级触发 |
| `skills/mstar-plan-conventions/` | `{HARNESS_DIR}` / `{PLAN_DIR}`、`status.json` SSOT、residual findings、knowledge/ 知识库、Done 瘦身、工期预估 |
| `skills/mstar-review-qc/` | QC 三审基线：工作流、清单、报告模板、门禁规则、residual 留档 |
| `skills/mstar-routing-eval/` | PM 路由回归 + prompt/规则迭代评估 + `Routing Eval Report` 模板；场景集在 `assets/routing-evals.json` |
| `skills/mstar-coding-behavior/` | 跨角色通用编码行为准则 |
| `skills/mstar-superpowers-align/` | Morning Star × Superpowers 对齐与消解 |
| `skills/mstar-roles/` | 角色提示词总线：角色正文集中在 `references/`，`agents/*.md` 仅保留 frontmatter + 参数绑定 |
| `.opencode/skills/mstar-host/` | OpenCode 宿主适配（库文档 Context7 协议、可选 MCP、`question` / `@explore` / `@general` 等 runtime 能力） |
| `.cursor/skills/mstar-host/` | Cursor 宿主适配（Task 并行 QC、单会话多帽） |
| `agents/*.md` | 角色绑定壳层：保留 frontmatter 与 `mstar-roles` 参数，不承载长正文 |
| `secrets.env.example` | 本仓库密钥变量示例（仅变量名与装载方式，不含真实值） |
| **本文件** | 维护边界、更新规则、设计审查；工作目录为本仓库时的默认任务理解 |

## 仓库边界

- 本目录为用户维护的**全局工具配置**，不是业务应用仓库。
- 当当前工作目录为 `~/.config/opencode`（或其子目录）时，默认任务目标是**维护本配置仓库**，而非外部项目。
- 仅可在用户明确授权下修改此处文件；优先先读后改。
- 在**业务**项目工作目录下执行时，以该项目 `AGENTS.md` / `CLAUDE.md` 为准；除非工作区仍是本仓库，否则本规则不适用。

## 维护原则

- **单一权威**（入口 + skill 索引）：根目录 `AGENTS.md`。深层流程正文放在 `skills/mstar-*/` 中；阶段或门禁变更时先更新对应 skill，仅当 skill 索引或摘要级不变量需对齐时再改根目录 `AGENTS.md`。
- 避免漂移：每个主题单一事实来源；`mstar-*` skill 的 `references/` 与 `SKILL.md` 正文间不要双写同一规则。
- 角色正文集中维护在 `skills/mstar-roles/references/`；`agents/*.md` 保持壳层最小化（frontmatter + role/reference/params）。
- **skill 是 SSOT，不是维护手册**：各 `mstar-*/SKILL.md` 与 `references/` 只承载**执行向**规则；**不得**把"如何修改本 skill / 如何跨文件同步 / 如何评审"之类**维护向**话术塞回 skill 正文——这些一律归入本文件。

## 快速导航

1. `AGENTS.md` — 全局入口
2. `skills/mstar-harness-core/SKILL.md` 与 `references/` — 核心不变量与分支 / worktree 约定
3. 路由或门禁变更后：按需更新相关 `mstar-*` skill 与根目录 `AGENTS.md`，并做一致性检查
4. **本文件** — 维护本配置仓库时的更新规则与设计审查清单

## 库文档与宿主

涉及第三方库 / API / CLI / 云服务语法、配置与迁移时，勿仅依赖模型训练数据。Context7 共享协议见 `mstar-harness-core` skill 的 `references/library-docs-protocol.md`；宿主差异仅在 host skills 维护。若变更该协议，请同步核对两侧 host skill 的指针描述。

## OpenCode 密钥与 `opencode.json` 维护（迁出 runtime skill）

> 说明：密钥相关内容属于**维护语义**，不再放在 runtime 的宿主 skill 中；统一在本文件维护。

`opencode.json` 支持占位替换（见 [OpenCode Config — Variables](https://opencode.ai/docs/en/config)）：

- `{env:VARIABLE_NAME}` — 从环境变量读取
- `{file:path}` — 从文件读取（路径可为 `~/.config/opencode/.secrets/...`）

### 本仓库示例所用环境变量

当使用仓库内 `opencode.example.json` 模板时，请在启动 OpenCode **之前**导出下列变量（名称与模板中的 `{env:...}` 一致）：

| 变量 | 用途 |
|------|------|
| `MINIMAX_API_KEY` | MiniMax API Key |
| `Z_AI_API_KEY` | 智谱 BigModel API Key |
| `MOONSHOT_API_KEY` | MoonShot(Kimi) API Key |
| `DASHSCOPE_API_KEY` | 阿里云百炼 / DashScope（`bailian-coding-plan` 的 `apiKey`） |
| `CONTEXT7_API_KEY` | Context7 API Key |

可复制 `secrets.env.example` 为本地 `secrets.env`（勿提交），执行 `set -a && source secrets.env && set +a` 后再启动 CLI/TUI。

若 `{env:...}` 在某字段未生效（已知部分版本对个别嵌套字段有问题），可改用 `{file:~/.config/opencode/.secrets/<name>}`，文件内容为**单行**密钥，`chmod 600`。

### 安全约束

- 不要将含真实密钥的 `opencode.json` 提交到 Git。
- 本仓库 `.gitignore` 已忽略 `secrets.env` 与 `.secrets/`；若你自行取消对 `opencode.json` 的忽略，务必改用 env 或 file 占位。
- **agent 不得擅自改** `opencode.json`、`secrets.env` 或 `.secrets/*`：如需变更，只提建议，由用户本人落盘。

## 修改 Skills 的工作流（Morning Star 系列）

**迁移后，"维护本仓库" ≈ "修改 mstar-\* skills"**。本节给出规范动作；路径、structure、description 触发语在 skill-creator 指南中有详解，本文不重复，仅强调 Morning Star 本地约束。

### Skill 目录结构（硬性）

```text
skills/mstar-<topic>/
├── SKILL.md          # 必含 YAML frontmatter（name + description）
├── references/       # 可选：SKILL.md 超 ~500 行时拆到这里
│   └── <subtopic>.md
└── assets/           # 可选：数据文件（如 routing-evals.json）
```

宿主专属 skill 例外路径：

- OpenCode: `.opencode/skills/mstar-host/`（结构同上）
- Cursor: `.cursor/skills/mstar-host/`（结构同上）

### 动手前先判清「在哪里改」

| 变更性质 | 落点 |
|---------|------|
| 核心不变量（状态机 / 两阶段门禁 / 分支 / worktree / QC-QA 对齐）措辞更新 | `skills/mstar-harness-core/SKILL.md` 或对应 `references/*.md` |
| Spec 级细节（`phase-gate-playbook` / `branch-and-worktree` / `open-harness-principles`） | 优先改 `mstar-harness-core/references/<file>.md`；SKILL.md 只动索引与摘要 |
| `status.json` / residual lifecycle / knowledge 布局 | 优先改 `mstar-plan-conventions/references/<file>.md`；SKILL.md 动索引 |
| QC 清单或报告模板 | `mstar-review-qc/SKILL.md`（该 skill 目前不拆 references/，除非超 500 行） |
| 路由回归场景 | `mstar-routing-eval/assets/routing-evals.json`；相关评分细则改 `SKILL.md` |
| Superpowers 对照表 | `mstar-superpowers-align/references/per-role-matrix.md` 或 `tension-table.md`；SKILL.md 仅改总体契约 |
| 角色行为正文（含重复角色共享） | `skills/mstar-roles/references/*.md` |
| 角色绑定参数（role_id / reviewer_index / track） | `agents/*.md`（仅壳层，不写长规则） |
| 宿主能力（OpenCode） | `.opencode/skills/mstar-host/SKILL.md` |
| 密钥占位 / `opencode.json` / `secrets.env.example` 维护 | **本文件**「OpenCode 密钥与 `opencode.json` 维护」小节 |
| Cursor 入口、Task 并行 QC、`/pm` | `.cursor/skills/mstar-host/SKILL.md` |
| 只涉及 skill 的触发短语 / 描述 | 只改该 skill `SKILL.md` 的 `description`（下节「description 纪律」） |
| 编排入口、优先级、skill 索引、护栏 | 根目录 `AGENTS.md`（注意与各 skill `description` 的一致性） |

### description 纪律（SKILL.md frontmatter）

- **保持 "pushy"**：description 是 skill 被触发的主要信号。参考 `~/.cursor/skills-cursor/create-skill/SKILL.md` 与 skill-creator 指南，包含"何时触发 / 做什么"两类线索，并列出具体触发短语。
- **每次修改正文时自检**：若正文新增了重要触发场景（例如 `mstar-review-qc` 新增 high-risk 运维清单），回头把关键词补进 `description`；否则 agent 可能在新场景漏触发。
- **不要把 description 写成摘要**：它首先是**触发器**，其次才是摘要。

### SKILL.md 体量管理

- **目标**：`SKILL.md` 正文 ≤ 500 行；超过时把分章节拆到 `references/`，`SKILL.md` 保留概述 + 索引。
- **当前拆分示范**：`mstar-plan-conventions`（5 refs）、`mstar-harness-core`（3 refs）、`mstar-superpowers-align`（2 refs）。保持该风格；勿在 `SKILL.md` 与 `references/` 同时重复完整规则。
- **references/ 文件命名**：`<subtopic>.md`（小写连字符），与 SKILL.md 中的索引短语对齐。

### 新增 / 拆分 / 重命名 skill

- **新增 mstar-* skill**：命名 `mstar-<topic>`；在 **根目录 `AGENTS.md` 的 skill 索引表** 与 `README.md`「Morning Star Skill 结构」表登记；若是角色相关能力，优先接入 `mstar-roles`（而非扩写 `agents/*.md`）。
- **拆分已有 skill**：两步走 — (1) 新 skill 落盘并通过 lint；(2) 在同一 PR 中更新 AGENTS.md、所有引用它的角色开场块、以及其他 skill 中跨引（grep `mstar-<old>`）。
- **重命名**：不推荐；若必须，走两 PR（新名落盘 + 留重定向说明；次 PR 删旧目录并清理引用）。

### 修改 skill 后的同步清单（每次勾选）

- [ ] **SKILL.md frontmatter `description`**是否仍能准确触发新场景？
- [ ] **`SKILL.md` 是否 ≤ 500 行**？若超，把新增内容拆到 `references/`，别让 SKILL.md 肥化。
- [ ] **跨 skill 引用**：本次变更是否影响其它 `mstar-*` skill 的交叉引用？`grep -rn "mstar-<this-skill>" skills/ .cursor/skills/ agents/ AGENTS.md README.md`。
- [ ] **`agents/*.md` 壳层参数**：本次变更是否需要更新 `Role reference` / `Role parameters`（而不是扩写正文）？
- [ ] **`AGENTS.md` skill 索引表**：skill 职责描述一行是否需要更新？
- [ ] **`README.md`「Morning Star Skill 结构」表**：一行摘要是否需要对齐？
- [ ] **README 多语言同步**：若更新 `README.md`，必须同步更新所有语言版本（当前至少包括 `README_CN.md`）；并检查跨语言链接（如 `Other languages`）可用且互相指向正确文件。
- [ ] **Lint**：`npx -y markdownlint-cli2`（glob 已覆盖 `skills/mstar-*/**/*.md`）。
- [ ] **路由回归**：若本次变更涉及 PM 路由或门禁，跑 `mstar-routing-eval` 的 `assets/routing-evals.json` 场景集。
- [ ] **Git 提交颗粒度**：skill 实体变更 + AGENTS.md 索引 + agent 壳层参数 + README 摘要 宜在同一 PR，避免 handoff 期间文档半同步。

### skill-creator 流程接入

- 新建或大改 skill 时，**优先** Read `~/.agents/skills/skill-creator/SKILL.md` 与 `~/.cursor/skills-cursor/create-skill/SKILL.md`，尊重其 progressive disclosure、description pushiness、references 布局约定。
- 如果要为本仓库的某个 `mstar-*` skill 跑 **description 优化回归**（避免描述漂移导致触发率变差），按 skill-creator 的 "Description Optimization" 小节生成 trigger eval queries；结果归档到 `skills/mstar-<topic>/evals/`（若启用）。本文件**不重复** skill-creator 内容。

## 更新规则（供维护全局配置的用户）

- **跨角色工作流、门禁或路由**变更时：先更新对应 `mstar-*` skill；若涉及角色行为正文，更新 `skills/mstar-roles/references/`；仅在参数变化时更新 `agents/*.md` 壳层。
- **状态机 / 两阶段门禁 / 并行规则 / 分支与 worktree** 变更时，优先更新 `skills/mstar-harness-core/SKILL.md` 与 `references/`（`phase-gate-playbook.md`、`branch-and-worktree.md`、`open-harness-principles.md`）。
- **评审策略**变更时，优先更新 `skills/mstar-review-qc/SKILL.md`。
- **QC 三审角色提示词**：共享正文维护在 `skills/mstar-roles/references/qc-specialist-shared.md`；`agents/qc-specialist*.md` 仅维护 frontmatter 与 reviewer 参数（`reviewer_index` / `focus` / `report_suffix`）。
- **提示词调优流程 / 路由回归** 变更时，优先更新 `skills/mstar-routing-eval/SKILL.md`；若场景集变更，同步 `skills/mstar-routing-eval/assets/routing-evals.json`。
- **Superpowers 与 harness 对照** 变更时：只更新 `skills/mstar-superpowers-align/`（含 `references/per-role-matrix.md`、`tension-table.md`）与必要的角色短指针；勿在各 `agents/*.md` 内嵌长对照表。
- **计划管理约定 / status.json / residual findings / knowledge/** 变更时，优先更新 `skills/mstar-plan-conventions/SKILL.md` 与对应 `references/`。
- **工期 / 工作量口径** 变更时，优先更新 `skills/mstar-plan-conventions/references/effort-estimation.md`，并同步 PM / product-manager / architect 模板与根目录 **`AGENTS.md``** Effort / 工期相关不变量或索引简述。
- **编码行为准则** 变更时，优先更新 `skills/mstar-coding-behavior/SKILL.md`；角色差异放在 `mstar-roles/references/`，不得回流到 `agents/*.md` 长正文。
- **库文档协议或宿主说明** 变更时，先更新 `skills/mstar-harness-core/references/library-docs-protocol.md`（共享协议），再同步 `.opencode/skills/mstar-host/SKILL.md`、`.cursor/skills/mstar-host/SKILL.md` 与根目录 **`AGENTS.md`*`**相关指针；核对 Cursor **Context7** 类规则（如 `~/.cursor/rules/context7.mdc`）是否一致。
- **密钥约定** 变更时，优先更新**本文件**「OpenCode 密钥与 `opencode.json` 维护」小节，并同步 `secrets.env.example` / `opencode.example.json`（若字段名或占位方式变更）。
- **共享入口、优先级、skill 索引或不变量** 变更时：只改**根目录 `AGENTS.md`**`**tar-*` skill 内部有独立 SKILL.md frontmatter `description`，同步更新触发语。
- **本维护规则**（边界、更新规则、审查清单）变更时：同步本文件 `.cursor/rules/opencode-config-repo-maintenance.mdc`。

## Agent 设计变更审查清单

- 根目录 `AGENTS.md`（全局入口）是否仍结构清晰、便于每会话加载？
- 角色职责是否与工作流细节分离（细节已在 `mstar-*` skill）？
- 每个 `mstar-*` skill 的 `description` 是否包含足够「pushy」的触发短语（见 skill-creator 指南）？
- 每个 SKILL.md 是否 ≤ 500 行？超限场景是否已拆到 `references/`？
- 每个阶段是否有可观察的具体产出？
- 反复出现的失败是否已转化为可复用指引（写回相关 `mstar-*` skill，而不是堆回 `agents/*.md`）？
- `AGENTS.md`、`skills/mstar-*/`、`agents/*.md` 是否仍夹带独立的「维护 / 跨文件同步 / 更新规则」长段或对本文件的重复说明？若有，应删除并并入本节「更新规则」或「修改 Skills 的工作流」，避免执行向文档与维护清单双轨。
- 各角色 `agents/*.md` 是否仍保持壳层最小化（frontmatter + role binding）？角色行为正文是否全部在 `skills/mstar-roles/references/` 维护并与参数化约定一致？
